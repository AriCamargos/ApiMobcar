import 'package:api_mobicar/models/category.dart';
import 'package:flutter/material.dart';

class OptionsScreens extends StatelessWidget {
  final List<MobCar> options;
  const OptionsScreens({required this.options});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
